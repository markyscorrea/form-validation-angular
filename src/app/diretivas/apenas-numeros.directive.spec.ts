import { ApenasNumerosDirective } from './apenas-numeros.directive';

describe('ApenasNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new ApenasNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
